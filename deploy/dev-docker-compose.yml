version: "3.9"

services:
  backend:
    image: ${IMAGE_BACKEND:-ghcr.io/B1ackC/backend-test:dev}
    container_name: backend-dev
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - backend-net

networks:
  backend-net:
    driver: bridge